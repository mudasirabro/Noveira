# Noveira — admin credentials for the demo dashboard.
# Copy to .env.local and change these values.
#
# NOTE: /admin is protected by a client-side guard only. These credentials
# gate the login form, not the route itself. Do not use this to protect
# anything sensitive.

ADMIN_EMAIL=admin@noveira.com
ADMIN_PASSWORD=change-me
